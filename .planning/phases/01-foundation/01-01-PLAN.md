---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tsconfig.json
  - src/app.controller.spec.ts
  - test/jest-e2e.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "pnpm test exits 0 with zero Jest references in any config file"
    - "NestJS DI resolves correctly — appController.getHello() returns 'Hello World!' inside the Vitest suite"
    - "TypeScript build (pnpm build) succeeds with noImplicitAny: true"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest runner config with unplugin-swc + pool:forks + globals:true"
      contains: "pool: 'forks'"
    - path: "package.json"
      provides: "Scripts use vitest; jest/ts-jest/@types/jest removed from devDependencies"
      contains: "\"test\": \"vitest run\""
    - path: "tsconfig.json"
      provides: "noImplicitAny: true enforced"
      contains: "\"noImplicitAny\": true"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "unplugin-swc auto-reads emitDecoratorMetadata"
      pattern: "swc.vite"
    - from: "src/app.controller.spec.ts"
      to: "NestJS DI"
      via: "Test.createTestingModule().compile()"
      pattern: "appController.getHello"
---

<objective>
Remove the entire Jest scaffold from the NestJS starter and replace it with Vitest 2.x using unplugin-swc so NestJS decorator metadata is preserved in test builds. Flip tsconfig.json to noImplicitAny: true for INFRA-03.

Purpose: pnpm test must work with zero Jest references before Phase 2 service tests are written.
Output: vitest.config.ts created; package.json clean of Jest; tsconfig strict; existing smoke test runs under Vitest.
</objective>

<execution_context>
@/Users/vishalvariyani/know-me/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vishalvariyani/know-me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

<interfaces>
<!-- Current package.json scripts and devDependencies that must change -->
<!-- Executor MUST read package.json before editing to see exact current structure -->

From package.json (current state):
- scripts.test = "jest"
- scripts.test:watch = "jest --watch"
- scripts.test:cov = "jest --coverage"
- scripts.test:debug = "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand"
- scripts.test:e2e = "jest --config ./test/jest-e2e.json"
- devDependencies includes: jest ^30.0.0, @types/jest ^30.0.0, ts-jest ^29.2.5
- Lines 54-70: "jest": { ... } config block — must be deleted entirely

From tsconfig.json (current state):
- "noImplicitAny": false  →  must become true
- "emitDecoratorMetadata": true  — must NOT change
- "experimentalDecorators": true  — must NOT change
- "module": "nodenext"  — vitest.config.ts must use module: { type: 'nodenext' }

From src/app.controller.spec.ts (current state):
- Already uses describe/it/expect/beforeEach without imports
- Uses Test.createTestingModule().compile()
- Content is already Vitest-compatible — no changes needed to test logic
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Jest scaffold and install Vitest + SWC</name>
  <files>package.json</files>
  <read_first>
    - package.json — read current scripts, devDependencies, and "jest" config block before editing
  </read_first>
  <action>
    Run the following commands in order:

    ```bash
    cd /Users/vishalvariyani/know-me
    pnpm remove jest @types/jest ts-jest
    pnpm add -D vitest@2.1.9 unplugin-swc @swc/core @vitest/coverage-v8
    ```

    Then edit package.json to:

    1. Replace the scripts section (scripts.test, test:watch, test:cov, test:debug, test:e2e) with:
       ```json
       "test": "vitest run",
       "test:watch": "vitest",
       "test:cov": "vitest run --coverage"
       ```
       Remove test:debug and test:e2e scripts entirely (they reference Jest internals with no Vitest equivalent needed now).

    2. Delete the entire "jest" config block (lines 54-70 in current file). The block looks like:
       ```json
       "jest": {
         "moduleFileExtensions": ["js", "json", "ts"],
         "rootDir": "src",
         ...
       }
       ```

    3. Delete test/jest-e2e.json — it has no Vitest equivalent and is referenced only from the deleted test:e2e script.

    Verify after: package.json must contain NO occurrence of the word "jest" anywhere (neither in scripts, devDependencies, nor as a top-level key).
  </action>
  <verify>
    <automated>cd /Users/vishalvariyani/know-me && grep -i "jest" package.json && echo "FAIL: jest still present" || echo "PASS: no jest references"; ls test/jest-e2e.json 2>/dev/null && echo "FAIL: jest-e2e.json still exists" || echo "PASS: jest-e2e.json deleted"</automated>
  </verify>
  <acceptance_criteria>
    - grep -i "jest" package.json returns no output (exit 1)
    - test/jest-e2e.json does not exist (ls returns error)
    - package.json contains "vitest run" as the test script value
    - vitest, unplugin-swc, @swc/core appear in devDependencies in package.json
  </acceptance_criteria>
  <done>Jest fully purged; Vitest installed; package.json scripts updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create vitest.config.ts and verify DI smoke test passes</name>
  <files>vitest.config.ts, tsconfig.json, src/app.controller.spec.ts</files>
  <read_first>
    - tsconfig.json — verify emitDecoratorMetadata:true and module:nodenext before writing vitest.config.ts
    - src/app.controller.spec.ts — confirm existing test structure; no content changes needed
  </read_first>
  <behavior>
    - Test: AppController DI resolves — appController.getHello() returns 'Hello World!' (not undefined)
    - Test: NestJS module compiles without throwing (if SWC metadata is missing, compile() throws or DI returns undefined)
  </behavior>
  <action>
    1. Create vitest.config.ts at the project root with this exact content:

    ```typescript
    import swc from 'unplugin-swc';
    import { defineConfig } from 'vitest/config';

    export default defineConfig({
      test: {
        globals: true,
        environment: 'node',
        pool: 'forks',
        include: ['src/**/*.spec.ts', 'test/**/*.spec.ts'],
      },
      plugins: [
        swc.vite({
          module: { type: 'nodenext' },
        }),
      ],
    });
    ```

    Critical: pool must be 'forks' — NOT 'threads' with singleThread:true (broken in Vitest 2.x, GitHub issue #6090). module.type must be 'nodenext' to match tsconfig.json "module": "nodenext".

    2. Edit tsconfig.json: change "noImplicitAny": false to "noImplicitAny": true. Do NOT change any other compiler flags (emitDecoratorMetadata, experimentalDecorators, module, moduleResolution must remain unchanged).

    3. src/app.controller.spec.ts requires NO changes — the existing test is already Vitest-compatible. Confirm by reading it before touching anything. If the content matches exactly (uses describe/it/expect without imports, calls appController.getHello()), leave it completely unchanged.

    4. Run the test suite to confirm NestJS DI works under SWC:
    ```bash
    cd /Users/vishalvariyani/know-me && pnpm test
    ```

    If pnpm test fails, diagnose: if "Cannot find module 'vitest/config'" — unplugin-swc not installed; if "appController is undefined" — SWC not emitting decorator metadata (check module.type in swc.vite options).

    5. Run build to confirm noImplicitAny:true does not break existing scaffold:
    ```bash
    cd /Users/vishalvariyani/know-me && pnpm build
    ```
  </action>
  <verify>
    <automated>cd /Users/vishalvariyani/know-me && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists at project root and contains pool: 'forks'
    - vitest.config.ts contains module: { type: 'nodenext' }
    - tsconfig.json contains "noImplicitAny": true
    - tsconfig.json still contains "emitDecoratorMetadata": true (unchanged)
    - pnpm test exits 0 with output showing "1 passed" or similar (the AppController smoke test)
    - pnpm build exits 0 (noImplicitAny:true does not break existing scaffold)
    - grep -i "jest" tsconfig.json returns no output (no jest references)
  </acceptance_criteria>
  <done>Vitest configured; SWC decorator metadata verified via smoke test; TypeScript strict enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → CI | Config files committed here run in CI — no secrets, only tooling config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | .env file | mitigate | Add .env to .gitignore in Plan 04; provide .env.example with placeholder values — .env must never be committed |
| T-01-01-02 | Tampering | tsconfig.json noImplicitAny | accept | Low risk — build-time flag only; no runtime impact; mitigated by pnpm build gate in verify |
</threat_model>

<verification>
After both tasks complete:
- `grep -i "jest" package.json` returns nothing
- `ls test/jest-e2e.json` returns "No such file"
- `cat vitest.config.ts` contains `pool: 'forks'` and `module: { type: 'nodenext' }`
- `grep "noImplicitAny" tsconfig.json` shows `"noImplicitAny": true`
- `pnpm test` exits 0
- `pnpm build` exits 0
</verification>

<success_criteria>
- Zero Jest references in package.json, tsconfig.json, or any config file
- pnpm test exits 0 and the AppController DI smoke test passes (confirming SWC decorator metadata is emitted)
- pnpm build exits 0 (noImplicitAny: true enforced, existing scaffold compiles clean)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template.
</output>
